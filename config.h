#pragma once

// Cài đặt màn hình
const int SCREEN_WIDTH = 128;
const int SCREEN_HEIGHT = 64;

// Cài đặt animation
const int TRANSITION_DURATION_MS = 700; // Thời gian chuyển đổi giữa các cảm xúc (0.7 giây)
const int PAUSE_DURATION_MS = 3000;      // Thời gian dừng lại ở mỗi cảm xúc (3 giây)